# Лог изменений проекта (Gemini CLI)

## [2026-02-13] Подготовка к деплою на Railway
- Создан `.gitignore` для защиты конфиденциальных данных и исключения тяжелых папок из репозитория.
- Обновлен `package.json`:
    - Добавлен скрипт `postinstall` для автоматической генерации Prisma Client (`prisma generate`).
    - Скрипт `start` дополнен командой `npx prisma migrate deploy` для применения миграций БД перед запуском.
- Создан `Procfile` для явного указания команды запуска процесса `web`.
- Сформулированы рекомендации по настройке переменных окружения (`DATABASE_URL`, `JWT_SECRET`).
- Предупреждение о необходимости использования Volume для папки `/uploads` на Railway.
